#include<iostream>
using namespace std;
// Multiplication of two numbers
int main() {
    int x,y,prod;
    cout<<"Enter x: ";
    cin>>x;
    cout<<"Enter y: ";
    cin>>y;

    prod = x * y;

    cout<<"Product of "<<x<<" and "<<y<<" is: "<<prod;
    return 0;
} 